# Development defaults for local Docker Compose usage.
PORT=8080
BACKEND_PORT=8080
FRONTEND_PORT=3000

POSTGRES_PORT=5432
POSTGRES_DB=idinex
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

DATABASE_URL=postgres://postgres:postgres@postgres:5432/idinex?sslmode=disable

JWT_SECRET=dev-secret

SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=

APP_ENV=development